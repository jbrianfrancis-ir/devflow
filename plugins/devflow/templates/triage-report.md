<!-- .planning/triage/<YYYY-MM-DDTHHMMSSZ>.md — generated by /flow-triage --export. A full timestamp,
not just the date, since a second export on the same day must never silently overwrite the first.
No size cap; one file per run. Every verdict here is copied from a flow-triager TRIAGE block, never
summarized further — a maintainer deciding whether to open a PR reads this instead of the
transcript, so it must carry the same detail the transcript did. -->
---
generated: {YYYY-MM-DDTHHMMSSZ}
repo: {owner/repo} {— or both origin and upstream repo names, when both were screened}
scope: {all open PRs | explicit PR numbers | external-authored only}
screened: {N}
merge_candidate: {N}
needs_human_judgment: {N}
needs_changes: {N}
reject: {N}
could_not_screen: {N}
---
# PR triage — {repo}, {YYYY-MM-DD}

{one line: what was screened and against what — "N open externally-authored PRs against
ARCHITECTURE.md, REQUIREMENTS.md, and conventions.md"}

## Flagged for review

| Repo | PR | Title | Author | Verdict | Reason |
|---|---|---|---|---|---|
| {owner/repo} | {#123} | {title} | {login} | {merge-candidate} | {one line} |

{full TRIAGE block per flagged PR — verbatim from the flow-triager that screened it}

## Everything else screened

| Repo | PR | Title | Author | Verdict | Reason |
|---|---|---|---|---|---|
| {owner/repo} | {#124} | {title} | {login} | {needs-changes} | {one line} |

{full TRIAGE block per row here only when the run carried --all; otherwise the table above is the
whole record for this section, and that is a complete result, not an abbreviated one — triage's
purpose is keeping these out of the flagged section, not hiding that they were seen.}

## Not screened

{PRs reported could-not-screen, with the specific reason from each flow-triager — "none" if empty.
Never omit this section even when empty; its absence would read as "nothing failed" rather than
"nothing to report".}

---
Generated by `/flow-triage --export`. Drafted responses above are output for a human to read, edit,
and post themselves — this report does not post, close, or merge anything.
