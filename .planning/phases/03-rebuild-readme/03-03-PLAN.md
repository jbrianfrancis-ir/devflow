---
phase: 03-rebuild-readme
plan: 03
wave: 3
depends_on: [03-02]
files_modified: [README.md, docs/execution-model.md, docs/providers.md]
autonomous: true
requirements: [REQ-03, SC-01]
must_haves:
  truths:
    - "README's opening is **one** paragraph of exactly the three kept sentences, byte-identical to the phase-start README: `awk '/^# DevFlow$/{f=1;next} /^## Install$/{exit} f && NF' README.md` prints exactly one line, that line is 345 bytes plus its newline (`wc -c` → 346), and `grep -qF` finds each of the three sentences in it. Length plus containment together prove nothing was added, reworded, or re-ordered — containment alone would pass on an opening with a fourth sentence bolted on."
    - "Each of the three displaced chunks has 0 hits in `README.md` and lands on exactly one `docs/` page: `nothing loads it all` → `docs/execution-model.md`; `azd on Azure` → `docs/execution-model.md`; `rather than replacing it` → `docs/providers.md`. Check per anchor: `grep -cF '<anchor>' README.md` → 0 and `grep -rlF '<anchor>' docs/` → that one file."
    - "D-14 holds at **every** commit of this plan, not only at its end — no window where the same prose lives in README and a page. Runnable, must print nothing: `shas=$(git log --format=%H --grep 'DevFlow-Plan: 03-03'); [ -n \"$shas\" ] || echo 'D-14 UNPROVEN: no commit carries DevFlow-Plan: 03-03'; for sha in $shas; do for pair in 'nothing loads it all|execution-model' 'azd on Azure|execution-model' 'rather than replacing it|providers'; do a=${pair%|*}; p=${pair#*|}; r=$(git show $sha:README.md | grep -cF \"$a\" || true); d=$(git show $sha:docs/$p.md 2>/dev/null | grep -cF \"$a\" || true); { [ \"$r\" -eq 0 ] && [ \"$d\" -gt 0 ]; } || { [ \"$r\" -gt 0 ] && [ \"$d\" -eq 0 ]; } || echo \"D-14 VIOLATION $sha [$a] README=$r PAGE=$d\"; done; done`. The `shas` guard is not decoration: a zero-iteration loop prints nothing, which this truth would otherwise read as a pass."
    - "The moved sentences are byte-identical to their phase-start originals — **including** the two numerals in the `nothing loads it all` sentence, which README already states correctly (`11 subagents`, `~220KB`; corrected at `e08ff1c` by phase 02's PR review). The sentence moves verbatim like every other: no numeral edit, and therefore **no deviation to log**. The numerals must still equal what the repo measures, and both commands are re-run at execution time: `ls plugins/devflow/agents/*.md | wc -l` → 11, and `find plugins/devflow/{agents,skills,references,templates} -name '*.md' -type f -exec cat {} + | wc -c` in KiB floored to the nearest 5 → 220. If either output differs from what README says, that is a real finding — stop and report it; do not silently edit a sentence this phase is moving. The `sed` substitution inside truth #5's reconstruction is not a licence to edit: it exists only because the pinned blob `38b2bc0` predates `e08ff1c` (`OPENING-MAP.md` §4)."
    - "Byte-identity is **graded, not asserted**: each moved sentence is reconstructed from the phase-start blob and `grep -qF` on its destination page — an anchor fragment proves a fragment landed, not that the sentence is unmodified, and `No Node runtime and no hooks.` has no anchor of its own at all. All three, each printing nothing:\n```\nB='git cat-file blob 38b2bc0'   # = 5ffe726:README.md at phase start, pinned BY BLOB so it still resolves after this branch is squash-merged and deleted\n# L3s3 → docs/execution-model.md; the sed only re-applies e08ff1c's numerals, which README already carries\nn=$(ls plugins/devflow/agents/*.md | wc -l)\nkb=$(( $(find plugins/devflow/{agents,skills,references,templates} -name '*.md' -type f -exec cat {} + | wc -c) / 1024 / 5 * 5 ))\nexp=$($B | awk 'NR==3' | sed 's/^.*\\(20 shared Agent Skills\\)/\\1/' | sed \"s/9 subagents/$n subagents/; s/~165KB/~${kb}KB/\")\n[ -n \"$exp\" ] || echo 'RECONSTRUCTION EMPTY (blob unreachable) — L3s3 UNGRADED'\ngrep -qF \"$exp\" docs/execution-model.md || echo 'L3s3 NOT BYTE-IDENTICAL'\n# L5s1+L5s2 → docs/execution-model.md, one line, single space between the two sentences\nexp=$($B | awk 'NR==5' | sed 's/ Claude invokes skills as.*$//')\n[ -n \"$exp\" ] || echo 'RECONSTRUCTION EMPTY (blob unreachable) — L5s1+L5s2 UNGRADED'\ngrep -qF \"$exp\" docs/execution-model.md || echo 'L5s1+L5s2 NOT BYTE-IDENTICAL'\n# L7s1 → docs/providers.md, minus the bold run-in label that became the heading\nexp=$($B | awk 'NR==7' | sed 's/^\\*\\*Orchestrator-agnostic\\.\\*\\* //; s/ Native subagents are the default\\..*$//')\n[ -n \"$exp\" ] || echo 'RECONSTRUCTION EMPTY (blob unreachable) — L7s1 UNGRADED'\ngrep -qF \"$exp\" docs/providers.md || echo 'L7s1 NOT BYTE-IDENTICAL'\n```\nAll three `sed` extractions were run against blob `38b2bc0` while writing this plan and return exactly the sentences §2 names (273, 163 and 76 bytes). **The `[ -n \"$exp\" ]` guard is load-bearing, exactly as the `shas` guard is one truth above:** `grep -qF \"\"` matches every non-empty file, so without it an unreachable blob greens all three checks silently — and `5ffe726` is reachable only from this branch and its remote, while this truth is a `must_have` the verifier re-runs after the branch is gone. Run against a dead SHA the guard fires three times; against `38b2bc0` it stays silent. A sentence with no check of its own is a sentence that can be reworded or dropped with every truth still green; a check that cannot go red is not a check."
    - "The four sentences `OPENING-MAP.md` §3 records rather than moves are gone from README and were **not** copied into `docs/`: `bounded peer role`, `without screen-scraping` and `Native subagents are the default` each read 0 hits in `README.md` **and** 0 files under `docs/`. Each row's cited destination still carries the claim — §3's four check commands all exit 0."
    - "Nothing else in README moved: `## Install` through the end of the file is byte-identical to the phase-start blob. `diff <(git show 5ffe726:README.md | sed -n '/^## Install$/,$p') <(sed -n '/^## Install$/,$p' README.md)` prints nothing — which also re-proves REQ-02's table (G6) for this plan."
    - "Gates G1–G4 pass after every commit: smoke exit 0 with `0 failures` and N >= 179 references (the moves drop README's two `docs/status-contract.md` references, so N lands near 188 — above the floor only because 03-02's index already landed); `docs/*.md` all <= 250 lines; `python3 scripts/check-fenced-paths.py` exit 0; `NOTICE` byte-identical."
    - "REQ-12/12a still holds on both edited pages: each keeps its `../plugins/devflow/references/…` link, and neither new section restates a normative rule from a reference — the moved prose is framing, not field lists or thresholds."
  backstop_truths: []
  artifacts: [docs/execution-model.md, docs/providers.md]
  key_links:
    - "`docs/execution-model.md` gains `## Context economy` (first H2) and `## Ship pipeline` (last H2) and keeps its links to `plan-format.md`, `verification.md`, `conventions.md`."
    - "`docs/providers.md` gains `## Orchestrator-agnostic` (first H2) and keeps its `hosts.md` link as source of truth."
---

<!-- Size note: ~14KB, over plan-format's 4KB cap on D-11's grounds. The per-commit D-14 proof and the
three sentence-reconstruction checks are the phase's evidence that a condense stayed a move; generalising
them to "the prose moved" is what this phase's audit successor cannot re-derive. The reconstructions are
verbatim on purpose — an anchor fragment cannot tell a moved sentence from a reworded one. -->

<objective>
Condense README's three opening paragraphs to the 2–3 sentence statement REQ-03 needs, moving the
displaced prose into the `docs/` pages that own those topics rather than deleting it (D-20).
</objective>

<context>
.planning/phases/03-rebuild-readme/OPENING-MAP.md (§2 sentence-by-sentence disposition, §3 recorded
claims, §4 the numeral correction, §5 anchors, §8 gates — read it first),
.planning/phases/03-rebuild-readme/CONTEXT.md (D-20), .planning/REQUIREMENTS.md (REQ-03, REQ-12, SC-01),
.planning/PROJECT.md (D-14, D-20), README.md, docs/execution-model.md, docs/providers.md,
docs/status-contract.md and docs/parallel-work.md (destinations cited in §3 — read, never edit)
</context>

<tasks>
<task type="auto">
  <name>Task 1: Move the token-economics and ship-pipeline prose to docs/execution-model.md</name>
  <files>docs/execution-model.md, README.md</files>
  <action>
Two new H2s on `docs/execution-model.md`:

- `## Context economy` — placed **first**, above `## Graph execution`. Body: README line 3's third
  sentence (anchor `nothing loads it all`), moved **verbatim** — numerals included. README already says
  `11 subagents` and `~220KB`; phase 02's PR review corrected them at `e08ff1c`, so there is nothing to
  edit here and **no deviation to log**. Still run the two commands in `OPENING-MAP.md` §4 and confirm
  they agree with what you are moving (`11` and `220`); if either disagrees, stop and report rather than
  editing the sentence.
- `## Ship pipeline` — placed **last**, after `## Design constraints`. Body: README line 5's first two
  sentences (`No Node runtime and no hooks.` and the `"Ship" is a real pipeline: …` sentence, anchor
  `azd on Azure`), moved intact, one paragraph — **both sentences on one line, separated by the single
  space README already has between them**, keeping the `[Aspire](https://aspire.dev)` link exactly as
  README has it. The one-line shape is not cosmetic: it is what makes the verify's reconstruction
  runnable, and `No Node runtime and no hooks.` has no anchor of its own, so that check is the only
  thing standing between it and quiet deletion.

In the **same commit** (D-14), delete both from README: line 3 keeps only its first two sentences, and
line 5 keeps only `Claude invokes skills as `/flow-*`; Codex invokes the same skills as `$flow-*`.`
Leave line 7 alone in this task — it is task 2's.

Nothing path-shaped goes inside a fenced block, and neither section needs a fence at all (D-15, G3).
REQ-12a: this is framing prose, not normative detail; do not add rules, and do not touch the page's
existing reference links.
  </action>
  <verify>
`grep -cF 'nothing loads it all' README.md` → 0; `grep -rlF 'nothing loads it all' docs/` → `docs/execution-model.md`. Same for `azd on Azure`.
Byte-identity, both chunks — reconstruct from the blob and match the whole sentence, not a fragment (each prints nothing):
```
B='git cat-file blob 38b2bc0'   # = 5ffe726:README.md at phase start, pinned by blob so the check outlives this branch
n=$(ls plugins/devflow/agents/*.md | wc -l)
kb=$(( $(find plugins/devflow/{agents,skills,references,templates} -name '*.md' -type f -exec cat {} + | wc -c) / 1024 / 5 * 5 ))
exp=$($B | awk 'NR==3' | sed 's/^.*\(20 shared Agent Skills\)/\1/' | sed "s/9 subagents/$n subagents/; s/~165KB/~${kb}KB/")
[ -n "$exp" ] || echo 'RECONSTRUCTION EMPTY (blob unreachable) — L3s3 UNGRADED'
grep -qF "$exp" docs/execution-model.md || echo 'L3s3 NOT BYTE-IDENTICAL'
exp=$($B | awk 'NR==5' | sed 's/ Claude invokes skills as.*$//')
[ -n "$exp" ] || echo 'RECONSTRUCTION EMPTY (blob unreachable) — L5s1+L5s2 UNGRADED'
grep -qF "$exp" docs/execution-model.md || echo 'L5s1+L5s2 NOT BYTE-IDENTICAL'
```
Without the `[ -n "$exp" ]` lines an unreachable blob makes `grep -qF ""` match anything and both checks green on a page that carries neither sentence.
`echo "$n / $kb"` → must print `11 / 220`, the numerals README already carries. Record them in the SUMMARY as a measurement, **not** as a deviation — nothing was edited, and a SUMMARY claiming otherwise would tell phase 04's REQ-06 audit that this phase modified a moved sentence.
Gates G1–G4 (`OPENING-MAP.md` §8): smoke exit 0, `0 failures`, N >= 179; `wc -l docs/*.md | awk …` silent; `python3 scripts/check-fenced-paths.py` exit 0; `NOTICE` diff exit 0.
  </verify>
  <done>Both chunks live on `docs/execution-model.md` under their new headings, neither remains in README, the numerals match measurement, and all four gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Move the orchestrator-agnostic sentence to docs/providers.md and drop the recorded four</name>
  <files>docs/providers.md, README.md</files>
  <action>
Add `## Orchestrator-agnostic` as the **first** H2 of `docs/providers.md`, above `## Provider selection`.
Body: README line 7's first sentence, `DevFlow runs as skills inside the interactive host rather than
replacing it.` — moved intact. The bold run-in label `**Orchestrator-agnostic.**` becomes the heading,
which is the phase-02 precedent for run-in labels; the sentence itself is not reworded.

In the same commit (D-14), delete README line 7 **in full** — the moved sentence and the four that
`OPENING-MAP.md` §3 records. Those four are not copied anywhere: every claim they carry is already
stated on a `docs/` page, and restating it would put one fact in two files, which `ARCHITECTURE.md`'s
principles call a defect. Before deleting, run §3's four check commands and confirm each exits 0 — if
one fails, the claim is *not* already recorded and you must stop and report rather than delete.

Do not edit `## Provider selection`'s existing paragraph — it is phase-02 prose and it is the richer
statement of the two (config key, precedence, authorization, checkpoints).
  </action>
  <verify>
`grep -cF 'rather than replacing it' README.md` → 0; `grep -rlF 'rather than replacing it' docs/` → `docs/providers.md`.
Byte-identity — the anchor proves a fragment landed, this proves the sentence is unreworded (prints nothing):
```
exp=$(git cat-file blob 38b2bc0 | awk 'NR==7' | sed 's/^\*\*Orchestrator-agnostic\.\*\* //; s/ Native subagents are the default\..*$//')
[ -n "$exp" ] || echo 'RECONSTRUCTION EMPTY (blob unreachable) — L7s1 UNGRADED'
grep -qF "$exp" docs/providers.md || echo 'L7s1 NOT BYTE-IDENTICAL'
```
The blob is pinned rather than reached through `5ffe726` so the check survives this branch being merged and deleted, and the non-empty guard is what keeps an unreachable blob from greening it anyway.
For each of `bounded peer role`, `without screen-scraping`, `Native subagents are the default`: `grep -cF` in README → 0 **and** `grep -rlF` in `docs/` → nothing. Both halves matter — a hit under `docs/` means the sentence was duplicated instead of recorded.
§3's four check commands → all exit 0.
`awk '/^# DevFlow$/{f=1;next} /^## Install$/{exit} f && NF' README.md` now prints two lines (task 3 merges them).
Gates G1–G4 → pass; the checker's N is at or above 179 (README lost its two `docs/status-contract.md` references here).
  </verify>
  <done>`docs/providers.md` opens with the orchestrator-agnostic framing, README's line 7 is gone in the same commit, and no recorded sentence was duplicated into `docs/`.</done>
</task>

<task type="auto">
  <name>Task 3: Close the opening to one paragraph and prove the condense</name>
  <files>README.md</files>
  <action>
Join what remains above `## Install` into a single paragraph, in order: the two surviving sentences of
line 3 followed by the surviving sentence of line 5, separated by single spaces, on one line — README's
paragraph style. No new words, no connectors, no re-punctuation: the result must be the concatenation
of three sentences that already exist in `git show 5ffe726:README.md`.

This is the plan's REQ-03 deliverable — the install blocks now sit within the first screen — and it is
the point where the phase's "a sentence that stays, stays byte-identical" rule is provable.
  </action>
  <verify>
`awk '/^# DevFlow$/{f=1;next} /^## Install$/{exit} f && NF' README.md > /tmp/open.txt; wc -l /tmp/open.txt` → 1; `wc -c /tmp/open.txt` → 346 (345 bytes plus newline).
`grep -qF 'Token-efficient spec-driven development for Claude Code and local Codex clients.' /tmp/open.txt`, `grep -qF 'goal-backward verification that abstains rather than guessing' /tmp/open.txt`, `grep -qF 'Codex invokes the same skills as' /tmp/open.txt` — all exit 0.
`diff <(git show 5ffe726:README.md | sed -n '/^## Install$/,$p') <(sed -n '/^## Install$/,$p' README.md)` → prints nothing (everything from `## Install` down is still untouched, table included).
`wc -l README.md` → <= 110 and `wc -c README.md` → <= 14000 (SC-01 — it will be well under; 03-04 adds the new sections).
The D-14 per-commit loop from the truths → prints nothing.
Gates G1–G4 → pass.
  </verify>
  <done>README opens with one 345-byte paragraph of three unmodified sentences directly above `## Install`, everything below `## Install` is byte-identical to phase start, and D-14 held at every commit of this plan.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 03-03`. -->
