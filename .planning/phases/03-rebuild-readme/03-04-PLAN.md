---
phase: 03-rebuild-readme
plan: 04
wave: 4
depends_on: [03-02, 03-03]
files_modified: [README.md]
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, REQ-04, SC-01, SC-04]
must_haves:
  truths:
    - "README has exactly six `##` sections and no others, in this order: `grep '^## ' README.md` prints `## Install`, `## Quick start`, `## Commands`, `## Configuration`, `## Documentation`, `## License and acknowledgements`, and `grep -c '^## ' README.md` → 6 (REQ-01). `### Claude Code` and `### Codex CLI, app, or IDE` remain as `###` and are not counted."
    - "SC-01: `wc -l README.md` <= 110 and `wc -c README.md` <= 14000."
    - "REQ-02: the command table is byte-identical to phase start and still 20 data rows. `diff <(git show 5ffe726:README.md | sed -n '/^| Loop | Command | Does |$/,/^$/p') <(sed -n '/^| Loop | Command | Does |$/,/^$/p' README.md)` prints nothing, and `sed -n '/^| Loop | Command | Does |$/,/^$/p' README.md | tail -n +3 | grep -c '^| '` → 20."
    - "`## Quick start` carries the first-run sequence in order and in commands that exist: `/flow-new`, `/flow-plan 1`, `/flow-execute 1` each appear in the section, their first mentions are in that order (compare `grep -n` line numbers), and every `/flow-` command the section names also appears in README's 20-row table. A Quick start naming a command the table does not is either a typo or a command that does not exist."
    - "The ASCII flow diagram survived the loss of `## Flow` byte-identically **with its fence**, and now sits inside `## Quick start`. Grading only the two ASCII lines (52–55's inner pair) leaves the fence ungraded: an executor who relocates the diagram unfenced passes that check, passes G3 (the diagram carries no repo path, so the guard is silent either way), and ships a broken render. So grade all four lines of `sed -n '52,55p'`, and grade them *as a block* — a bare `grep -qxF '```'` proves only that README has some fence somewhere, which it does regardless (the install blocks). Measured against a scratch README with lines 52 and 55 stripped: the four-line loop stays silent, the window diff reds. Prints nothing:\n```\nB='git cat-file blob 38b2bc0'   # = 5ffe726:README.md at phase start, pinned by blob so it outlives this branch\nblk=$($B | sed -n '52,55p')\n[ -n \"$blk\" ] || echo 'BLOB UNREACHABLE — diagram checks UNGRADED'\nprintf '%s\\n' \"$blk\" | while IFS= read -r l; do grep -qxF \"$l\" README.md || echo \"DIAGRAM CHANGED: $l\"; done\na=$($B | sed -n '53p'); d=$(grep -nxF \"$a\" README.md | head -1 | cut -d: -f1)\n[ -n \"$d\" ] || echo 'DIAGRAM ANCHOR NOT FOUND'\ndiff <(printf '%s\\n' \"$blk\") <(sed -n \"$((d-1)),$((d+2))p\" README.md) || echo 'DIAGRAM BLOCK CHANGED (fence or content)'\n```\nAnd `$d` falls between the `## Quick start` and `## Commands` heading line numbers."
    - "SC-04's mechanical half: no step of `## Quick start` requires opening `docs/` to know what to type — every instruction in the section is complete in README, and any `docs/` link there is offered as depth, not as a step. Proxy only; the reader half is the human check below and a green proxy does not close it."
    - "`## Configuration` names `--provider` with its three values and `.planning/config.json`, and links `docs/providers.md`. Every config key it names is one the repo actually defines, checked by task 2's resolver. A dotted `grep -rF '<key>'` is **not** that check and must not be substituted for it: the repo writes these keys as nested JSON, never dotted, so `deploy.tool`, `autonomy.max_hours`, `git.origin` and `git.upstream` all return nothing to it while being real (measured) — and `grep -rqF mode` matches `model`/`modes` everywhere, so the shortest key would pass no matter what. A check that reds on real keys and greens on invented ones grades nothing. An invented key in the README is a support burden no link check catches."
    - "`## Documentation` links `docs/README.md` and that link resolves, so every topic phase 02 removed is reachable from README in two clicks (REQ-01's accept criterion, REQ-04's index)."
    - "`## License and acknowledgements` keeps phase 02's pointer line naming both `docs/acknowledgements.md` and `NOTICE`, byte-identical, and `tail -1 README.md` is still `MIT licensed — see `LICENSE`.` (REQ-07 unchanged by this phase)."
    - "Gates G1–G7 from `OPENING-MAP.md` §8 all pass at this plan's final commit: smoke exit 0 with `0 failures` and **N >= 190** references checked (up from 179 at phase start — the phase added inbound links and dropped none); `docs/*.md` <= 250 lines; `python3 scripts/check-fenced-paths.py` exit 0 with README in scope, since this plan adds fenced blocks to it; `NOTICE` byte-identical."
    - "Nothing outside README moved in this plan: `git diff --exit-code $(git merge-base main HEAD) -- docs/blitzos.md docs/status-contract.md plugins/devflow/ NOTICE LICENSE` exits 0."
  backstop_truths: []
  artifacts: [README.md]
  key_links:
    - "README `## Documentation` → `docs/README.md` → all eleven `docs/` pages (the only path from README to the eight pages phase 02 orphaned)."
    - "README `## Configuration` → `docs/providers.md` for provider dispatch and model tiers; `docs/autonomy.md` for the autonomy rails."
    - "README `## License and acknowledgements` → `docs/acknowledgements.md`, and names `NOTICE`."
---

<!-- Size note: over plan-format's 4KB cap on D-11's grounds. This plan closes REQ-01, REQ-03, SC-01 and
SC-04 at once, and each truth carries the command that grades it — including the one that says out loud
what the SC-04 proxy does not prove. -->

<objective>
Write the two fresh sections D-18 sanctions (`## Quick start`, `## Configuration`), add `## Documentation`,
and land README on exactly REQ-01's six sections within SC-01's caps.
</objective>

<context>
.planning/phases/03-rebuild-readme/OPENING-MAP.md (§6 target shape, §8 gates, §10 the SC-04 honesty note),
.planning/phases/03-rebuild-readme/CONTEXT.md (D-18, and SC-04 under Watch out), .planning/REQUIREMENTS.md,
README.md, docs/README.md, docs/providers.md, docs/autonomy.md, docs/execution-model.md,
plugins/devflow/skills/flow-new/SKILL.md (the `config.json` shape it writes — the authority for the keys
you name), plugins/devflow/skills/flow-plan/SKILL.md, plugins/devflow/skills/flow-execute/SKILL.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Write ## Quick start and dissolve ## Flow</name>
  <files>README.md</files>
  <action>
Insert `## Quick start` between `## Install` and `## Commands`, and delete the `## Flow` heading, moving
its fenced ASCII diagram into this section byte-identically (it is the phase's only relocated fence, and
`scripts/check-fenced-paths.py` now covers README).

Content — a numbered first-run walkthrough that stands alone (SC-04). Minimum shape:
1. `/flow-new` in the repo you want to work in (new or existing) — what it asks for and what it writes
   (`.planning/` requirements, roadmap, state; the feature branch it records).
2. `/flow-plan 1` — discuss the phase's decisions, then plans get written and checked.
3. `/flow-execute 1` — plans run in waves and the phase is verified at the end.
4. Repeat 2–3 per phase; `/flow-status` at any point prints where you are and the exact next command.
5. `/flow-pr` when a phase is verified.
Then the diagram, then at most one depth link (`docs/execution-model.md` for what happens inside a
phase). Keep it under ~15 lines: D-18 says minimal, and every line here competes with the install
blocks for the first screen (REQ-03).

**Accuracy over prose.** Every claim about what a command does must match that skill's own
`SKILL.md` description and the row already in README's table — read them, do not paraphrase from
memory, and name no flag you have not confirmed. Do not touch the table itself (REQ-02).
  </action>
  <verify>
`grep -n '^## ' README.md` shows Install, Quick start, Commands in that order and no `## Flow`.
The diagram moved with its fence — all four lines of 52–55, graded as a block (prints nothing):
```
B='git cat-file blob 38b2bc0'   # = 5ffe726:README.md at phase start
blk=$($B | sed -n '52,55p')
[ -n "$blk" ] || echo 'BLOB UNREACHABLE — diagram checks UNGRADED'
printf '%s\n' "$blk" | while IFS= read -r l; do grep -qxF "$l" README.md || echo "DIAGRAM CHANGED: $l"; done
a=$($B | sed -n '53p'); d=$(grep -nxF "$a" README.md | head -1 | cut -d: -f1)
[ -n "$d" ] || echo 'DIAGRAM ANCHOR NOT FOUND'
diff <(printf '%s\n' "$blk") <(sed -n "$((d-1)),$((d+2))p" README.md) || echo 'DIAGRAM BLOCK CHANGED (fence or content)'
```
The window diff is the one that grades the fence: the `grep -qxF` loop alone passes on an unfenced relocation, because ``` matches README's install fences. `grep -n` must put `$d` between the `## Quick start` and `## Commands` headings.
Every command Quick start names is in the table: `tbl=$(sed -n '/^| Loop | Command | Does |$/,/^$/p' README.md); qs=$(awk '/^## Quick start$/{f=1;next} /^## /{f=0} f' README.md); for c in $(printf '%s' "$qs" | grep -oE '/flow-[a-z-]+' | sort -u); do printf '%s' "$tbl" | grep -qF "$c" || echo "NOT IN TABLE: $c"; done` → prints nothing.
G6 (table byte-identical, 20 rows) and G3 (`python3 scripts/check-fenced-paths.py`, exit 0) both pass.
  </verify>
  <done>`## Quick start` gives a first-time reader the whole first run without leaving README, the diagram moved intact, `## Flow` is gone, and the table is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write ## Configuration</name>
  <files>README.md</files>
  <action>
Insert `## Configuration` after `## Commands`. Minimal (D-18) — summary level, then link out:

- `--provider native|claude|codex` on the delegating skills, and the project default
  `"agents": {"provider": …}` in `.planning/config.json`, with the flag winning over the file.
- The other keys `.planning/config.json` carries, named not explained: `mode`, `commit_docs`,
  `agents.models.<role>`, `autonomy.{max_iterations,max_repeats,max_hours}`,
  `git.{base,origin,upstream,branch}`, `deploy.tool`.
- Links: `docs/providers.md` for provider dispatch and model tiers, `docs/autonomy.md` for the rails.

Verify each key against the repo before writing it — `plugins/devflow/skills/flow-new/SKILL.md` is the
authority for the block `/flow-new` writes, `docs/providers.md` for `agents.models`, `docs/autonomy.md`
for the `autonomy` block. No exact thresholds, no defaults table, no JSON schema: that detail lives in
`references/` and the pages that link it, and README restating it is the stale-duplicate failure
`ARCHITECTURE.md` → Principles names. Aim for 6–8 lines.
  </action>
  <verify>
The section names `--provider`, all three provider values, and `.planning/config.json`, and links `docs/providers.md`.
Every key the section names is one the repo defines — resolved against the shipped config first, then
against a line that names the block *and* the leaf in key form. Set `KEYS` to exactly the keys you wrote,
character for character — including the `<role>` placeholder in `agents.models.<role>`, which is how both
`plugins/devflow/references/hosts.md:69` and `docs/status-contract.md:34` write it and therefore how it
resolves. Leave `PROBES` alone. Prints nothing:
```
python3 - <<'EOF'
import json, pathlib, re
KEYS = "mode commit_docs agents.provider agents.models.<role> autonomy.max_iterations autonomy.max_repeats autonomy.max_hours git.base git.origin git.upstream git.branch deploy.tool".split()
PROBES = ["agents.model", "autonomy.max_cost", "git.remote", "deploy.region", "verbose"]   # inventions: all five must be rejected ("verbose" exercises the single-segment branch)
cfg = json.loads(pathlib.Path(".planning/config.json").read_text())
lines = [l for d in ("plugins/devflow/skills", "plugins/devflow/references", "docs")
           for f in pathlib.Path(d).rglob("*.md") for l in f.read_text().splitlines()]
def leaf_re(leaf, parent):
    alts = [re.escape('"%s"' % leaf), re.escape('`%s`' % leaf)]
    if parent: alts.append(re.escape('%s.%s' % (parent, leaf)) + r'(?![A-Za-z0-9_])')
    return re.compile('|'.join(alts))
def parent_re(parent):
    return re.compile('|'.join([re.escape('"%s"' % parent), re.escape('`%s`' % parent), re.escape(parent + '.')]))
def defined(key):
    parts = key.split('.'); node = cfg
    for seg in parts:                                  # 1. the config .planning/config.json actually carries
        if isinstance(node, dict) and seg in node: node = node[seg]
        else: break
    else: return True                                  #    (presence, not truthiness — upstream/tool are null)
    nested = len(parts) > 1                            # 2. or one line names the block and the leaf as a key
    lf, pr = leaf_re(parts[-1], '.'.join(parts[:-1]) if nested else None), parent_re(parts[0])
    return any(lf.search(l) and (not nested or pr.search(l)) for l in lines)
for k in KEYS:
    if not defined(k): print("INVENTED KEY:", k)
for p in PROBES:
    if defined(p): print("CHECK IS NOT FALSIFIABLE — it accepts", p)
EOF
```
Two details are load-bearing. `leaf_re`'s parent is `'.'.join(parts[:-1])`, not `parts[0]` — for a
three-segment key the latter builds `agents.<role>`, a string that appears nowhere, so the resolver would
red on the very key this action tells you to write. And `PROBES` carries a **single-segment** invention:
that branch skips the parent check entirely, so without one the probes never exercise it and bare words
like `models` or `provider` resolve as "defined" on nothing more than a backtick somewhere in `docs/`.
`verbose` returns False today, which is what makes the branch falsifiable.

The `PROBES` half is the point: a key check that cannot fail is not a check. All five are plausible
inventions (`agents.model` is the singular of a real key, `git.remote` is the name another tool would
use, `verbose` is the flag every tool has), and the whole block was run against this tree while planning
— all twelve real keys resolve (the action names `git.branch` too, so the template carries it), all
five probes are rejected.
G1 smoke exit 0, `0 failures`, N >= 190.
  </verify>
  <done>`## Configuration` covers the provider flag and the config keys at summary level in under ten lines and links the two pages that own the depth.</done>
</task>

<task type="auto">
  <name>Task 3: Add ## Documentation, rename the last section, and close REQ-01 / SC-01</name>
  <files>README.md</files>
  <action>
Insert `## Documentation` after `## Configuration`: one or two lines pointing at
`[docs/README.md](docs/README.md)` as the index of every topic page, optionally naming two or three
destinations a reader most often wants. Do not re-list all eleven — that is the index's job and a second
list drifts.

Rename `## Acknowledgements` to `## License and acknowledgements` (REQ-01's sixth slot,
`OPENING-MAP.md` §6). Its body does not change: phase 02's pointer line naming `docs/acknowledgements.md`
and `NOTICE` stays byte-identical, and `MIT licensed — see `LICENSE`.` stays the last line of the file.

Then run the full gate set and land SC-01. If README is over 110 lines or 14000 bytes, cut from the new
sections — never from the table (REQ-02), never from the opening (03-03 pinned it byte-for-byte).
  </action>
  <verify>
G5: `grep -c '^## ' README.md` → 6; `grep '^## ' README.md` → the six of `OPENING-MAP.md` §6 in order; `wc -l README.md` <= 110; `wc -c README.md` <= 14000.
G6: table diff empty, 20 rows. G1: smoke exit 0, `0 failures`, N >= 190. G2, G3, G4: clean.
The acknowledgements pointer and the MIT line, both byte-identical — same shape and same guard as truth #5's diagram check, because a `while read` loop over an unreachable blob iterates zero times and prints nothing, which reads as a pass (prints nothing):
```
two=$(git cat-file blob 38b2bc0 | sed -n '59p;61p')   # = 5ffe726:README.md at phase start
[ "$(printf '%s\n' "$two" | wc -l)" -eq 2 ] || echo 'BLOB UNREACHABLE — REQ-07 lines UNGRADED'
printf '%s\n' "$two" | while IFS= read -r l; do grep -qxF "$l" README.md || echo "CHANGED: $l"; done
```
`tail -1 README.md` is the MIT line.
`git diff --exit-code $(git merge-base main HEAD) -- docs/blitzos.md docs/status-contract.md plugins/devflow/ NOTICE LICENSE` → exit 0.
<human-check>
SC-04, the half no command establishes: read README from the top as someone who has never seen DevFlow,
following no `docs/` link. Can you install it and get through `/flow-new` → `/flow-plan 1` →
`/flow-execute 1` on a real repo with only what README told you? Note any step where you had to guess or
open a `docs/` page — that is an SC-04 gap, not a wording nit.
</human-check>
  </verify>
  <done>README is exactly REQ-01's six sections inside SC-01's caps, the index is reachable from it, the acknowledgements and MIT lines are unchanged, and the SC-04 reader check is queued for the human batch.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 03-04`. -->
