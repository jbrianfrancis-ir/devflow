---
phase: 01-link-safety-net
plan: 03
wave: 3
depends_on: [01-01, 01-02]
files_modified: [.planning/ARCHITECTURE.md, .github/workflows/lint.yml]
autonomous: true
requirements: [REQ-10, REQ-11, SC-02, SC-05]
must_haves:
  truths:
    - "The `## Smoke` command in .planning/ARCHITECTURE.md, copied verbatim and run, exits 0 with unittest reporting OK — including 01-02's test_check_links suite — and the checker printing no failure lines."
    - "That command names three steps: validate-plugin.py, unittest discover, check-links.py."
    - "`.github/workflows/lint.yml` has a step running `python3 scripts/check-links.py`; that command exits 0 at HEAD and non-zero when a backticked `scripts/nope.py` is appended to README.md; `git checkout README.md` restores exit 0."
    - "`grep -c 'uses:' .github/workflows/lint.yml` returns 1 — no action added beyond actions/checkout@v4 (SC-05)."
    - "lint.yml still triggers on push to `main` and on pull_request (REQ-10)."
    - "No install, pip, or setup-python step exists anywhere in .github/workflows/lint.yml."
  artifacts: []
  key_links:
    - ".planning/ARCHITECTURE.md `## Smoke` command ends with `&& python3 scripts/check-links.py`."
    - "lint.yml runs the link check inside the existing `validate` job."
---

<objective>
Make the link check a standing gate: part of the repo's smoke command and part of CI, so every
later phase re-proves link integrity as a condition of its own verification.
</objective>

<context>
.planning/ARCHITECTURE.md (`## Smoke` is the only editable section here; `## Link checking` is read-only
context), .planning/REQUIREMENTS.md (REQ-11 wording),
.planning/phases/01-link-safety-net/CONTEXT.md,
.planning/phases/01-link-safety-net/01-01-SUMMARY.md (the implemented rule set and any residue it classified),
.planning/phases/01-link-safety-net/01-02-SUMMARY.md (how the fixture tests drive `check(root)`),
scripts/check-links.py, tests/test_check_links.py, .github/workflows/lint.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend ARCHITECTURE.md ## Smoke</name>
  <files>.planning/ARCHITECTURE.md</files>
  <action>
Pre-flight (REQ-11): confirm `scripts/check-links.py` exists at HEAD and exits 0. If it does not, stop and
report — REQ-11 forbids this edit landing earlier than the commit that makes the script runnable, and a
smoke command naming a missing file breaks every earlier phase for the wrong reason (CONTEXT.md → Watch
out). Landing in this later commit of the same phase is what REQ-11 allows, provided smoke exits 0 here
and at every intermediate commit.

Append `&& python3 scripts/check-links.py` to the `## Smoke` **Command** line. Extend **Pass looks like**
to require exit 0 from all three steps and no failure lines from the checker. Delete the HTML comment
below `## Smoke` that reserved this edit — it is now satisfied.

Touch nothing else in ARCHITECTURE.md. `## Link checking` is a human-owned hard constraint and REQ-11
authorizes only the `## Smoke` edit: read that section as the contract 01-01 was built against, and if the
implementation appears to contradict it, raise a `checkpoint:decision` rather than editing it.
  </action>
  <verify>Copy the edited Smoke Command line verbatim and run it: exits 0, unittest reports OK, checker prints nothing. `git diff .planning/ARCHITECTURE.md` touches only the `## Smoke` section (Command line, Pass looks like, and the deleted reservation comment) — no hunk falls in `## Link checking`.</verify>
  <done>Smoke names all three steps and passes; the reservation comment is gone; `## Link checking` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Run the link check in CI</name>
  <files>.github/workflows/lint.yml</files>
  <action>
Add one step to the existing `validate` job, after the two current steps, named for what it does, running
`python3 scripts/check-links.py`. Nothing else changes: same `runs-on: ubuntu-latest`, same
`actions/checkout@v4`, same `on: push: branches: [main]` + `pull_request` triggers, which already satisfy
REQ-10. Add no action and no setup or install step — `python3` is present on the runner and
ARCHITECTURE.md forbids anything beyond `actions/checkout@v4` (SC-05).
  </action>
  <verify>`grep -n 'check-links' .github/workflows/lint.yml` shows the step; `grep -c 'uses:'` returns 1; `grep -nE 'pip|install|setup-python' .github/workflows/lint.yml` returns nothing. Run all three step commands in order locally — each exits 0. Append a backticked `scripts/nope.py` to README.md, confirm the link command exits non-zero, then `git checkout README.md`.
  <human-check>On the PR for this phase, confirm `lint` ran the link step and passed, then that a commit carrying a deliberately broken internal reference turns `lint` red before it is reverted — REQ-10's acceptance is only observable on a real PR.</human-check></verify>
  <done>lint.yml runs the checker with no new action or install step; the same commands are green locally.</done>
</task>
</tasks>

<!-- Commits: per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 01-03`. -->
