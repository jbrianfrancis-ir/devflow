---
phase: 02-carve-out-docs
plan: 05
wave: 5
depends_on: [02-04]
files_modified: [README.md, docs/autonomy.md, docs/acknowledgements.md]
autonomous: true
requirements: [REQ-05, REQ-07, REQ-12, REQ-12a, REQ-12b, REQ-12c, SC-03]
must_haves:
  truths:
    - "Each of these phrases has 0 hits in `README.md` and appears in exactly one file under `docs/`: `machine-checkable status line`, `Sweep the fleet`, `honor it all night`, `answer from anywhere`, `never auto-proceed`, `cheap convenience` (→ `docs/autonomy.md`); `exogenous abstention`, `oracle source files`, `graph-engineering framing`, `specification discipline`, `foreman and crew`, `four-session manual relay`, `AgentOS blueprint`, `readiness-over-sleeps` (→ `docs/acknowledgements.md`)."
    - "The acknowledgements moved **verbatim**: `git show e23403c:README.md | sed -n '151,165p' | grep -v '^$' | while IFS= read -r l; do grep -qxF \"$l\" docs/acknowledgements.md || echo NOT-VERBATIM; done` prints nothing — all 8 source paragraphs appear as byte-identical whole lines on the page (REQ-07)."
    - "`NOTICE` is byte-identical: `git diff --exit-code $(git merge-base main HEAD) -- NOTICE` exits 0 and prints nothing."
    - "README keeps a `## Acknowledgements` heading whose body is a single pointer line naming both `docs/acknowledgements.md` and `NOTICE`, and still ends with the `MIT licensed — see LICENSE.` line (REQ-07, REQ-01)."
    - "D-14 holds at **every** commit of this plan, not only at its end. Enumerate this plan's commits with `git log --format=%H --grep 'DevFlow-Plan: 02-05'` (the trailer this plan tags its commits with — see the commit note at the foot of this file), and for each sha and each anchor→page pair require the anchor to appear in exactly one of the two files — non-zero hits on one side, zero on the other. Hits on both means the prose sits in both files — the copy-now-delete-later window D-14 forbids; zero on both means it was dropped. Runnable, must print nothing: `shas=$(git log --format=%H --grep 'DevFlow-Plan: 02-05'); [ -n \"$shas\" ] || echo \"D-14 UNPROVEN: no commit carries DevFlow-Plan: 02-05\"; for sha in $shas; do for pair in 'machine-checkable status line|autonomy' 'Sweep the fleet|autonomy' 'honor it all night|autonomy' 'answer from anywhere|autonomy' 'never auto-proceed|autonomy' 'cheap convenience|autonomy' 'exogenous abstention|acknowledgements' 'oracle source files|acknowledgements' 'graph-engineering framing|acknowledgements' 'specification discipline|acknowledgements' 'foreman and crew|acknowledgements' 'four-session manual relay|acknowledgements' 'AgentOS blueprint|acknowledgements' 'readiness-over-sleeps|acknowledgements'; do a=${pair%|*}; p=${pair#*|}; r=$(git show $sha:README.md | grep -cF \"$a\" || true); d=$(git show $sha:docs/$p.md 2>/dev/null | grep -cF \"$a\" || true); { [ \"$r\" -eq 0 ] && [ \"$d\" -gt 0 ]; } || { [ \"$r\" -gt 0 ] && [ \"$d\" -eq 0 ]; } || echo \"D-14 VIOLATION $sha [$a] README=$r PAGE=$d\"; done; done`. The invariant is which side is non-zero rather than a sum: `grep -c` counts matching *lines*, so a correct page that carries an anchor on two lines — a sub-heading plus the prose, or one sentence per line — would fail a sum test. It is one-sided rather than a flat README=0 / page≥1 because each anchor moves in exactly one of this plan's commits; at the last sha every pair reads README=0, page≥1. The `shas` guard is not decoration: a zero-iteration loop prints nothing, which this truth would otherwise read as PASS, so a forgotten or mistyped trailer would silently prove nothing (fail-closed guards, conventions.md). `git show --stat` is not sufficient evidence here — it proves both files were touched, not that the README delta removed the prose the page gained."
    - "After every commit, `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` exits 0, and the checker's last line reports `0 failures` with at least 140 references checked."
    - "G3 from `.planning/phases/02-carve-out-docs/MAPPING.md` prints nothing across every page this phase wrote (D-15)."
    - "`wc -l docs/*.md | awk '$2 != \"total\" && $1 > 250'` prints nothing (SC-03)."
    - "At this plan's final commit, README's only remaining `##` headings are Install, Commands, Flow and Acknowledgements (`grep -c '^## ' README.md` → 4), and `ls docs/*.md` lists eleven files: the nine this phase created — acknowledgements, autonomy, execution-model, installation, parallel-work, providers, provenance, requirements-clarity, review — plus the untouched blitzos and status-contract."
    - "`git diff --exit-code $(git merge-base main HEAD) -- plugins/devflow/references/ docs/blitzos.md docs/status-contract.md` exits 0 (REQ-12c, out-of-scope files untouched)."
  artifacts: [docs/autonomy.md, docs/acknowledgements.md]
  key_links:
    - "README's Acknowledgements pointer links `docs/acknowledgements.md` — from README that path resolves; the page itself adds no reference links (REQ-12b owns it)."
    - "`docs/autonomy.md` links `../plugins/devflow/references/autonomy.md` as source of truth for the status-line grammar, rail semantics and gate list."
---

<!-- Size note: this plan is ~10KB against plan-format's 4KB cap. Deliberate, on the same grounds
D-11 accepted for phase 01: the per-anchor move truths and the per-commit D-14 proof are what cost the
bytes, and they are what makes the phase verifiable by an executor with no memory of the planning
conversation. Do not shrink it by generalizing them. -->

<objective>
Carve autonomous operation and session hygiene into docs/autonomy.md, then move the acknowledgements
verbatim to their own page, leaving README a one-line pointer. Last plan of the phase.
</objective>

<context>
.planning/phases/02-carve-out-docs/MAPPING.md (the section→file mapping, gate commands G1–G4,
anchor phrases, and the measured link forms — read it first),
.planning/phases/02-carve-out-docs/CONTEXT.md, .planning/REQUIREMENTS.md, README.md, NOTICE,
plugins/devflow/references/autonomy.md, plugins/devflow/references/checkpoints.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Carve docs/autonomy.md</name>
  <files>docs/autonomy.md, README.md</files>
  <action>
Create `docs/autonomy.md` with an `# Autonomous operation` heading from README's whole
`## Autonomous operation` section plus its `## Session hygiene (/clear)` section, located by anchor
phrase, in this order:
1. The status line and what `/goal` can verify — `machine-checkable status line`.
2. The five recipes, as the same bullet list — `Sweep the fleet`.
3. Loop rails — `honor it all night` (stuck signature, iteration cap, optional wall clock; an absent
   `## Run` block is a cold start, a malformed one is `BLOCKED`).
4. Gates you can answer from anywhere — `answer from anywhere` (the structured `## Gate` block as the
   one exception to "never parse skill prose"; a driver may surface options, never pick one).
5. Human gates that never auto-proceed, with the cost note — `never auto-proceed`.
6. Session hygiene — `cheap convenience`, with both bullets. D-16 folds it here: its operative half
   is the rule that you do not `/clear` mid-run.
Remove both README sections **and both their headings** in this same commit (D-14).

REQ-12: `autonomy.md` in `references/` owns the status-line grammar, the rail semantics and the
authoritative gate list; `checkpoints.md` owns the checkpoint types. Keep the reasoning — `CONTINUE`
means the next command can run, not that the run is getting anywhere; a counter that can't be read is
not a counter that says zero — and link `../plugins/devflow/references/autonomy.md` rather than
restating the gate list as normative text. Session hygiene has no reference counterpart and is owned
outright (REQ-12b).

The recipes keep their inline code spans (`/goal FLOW says DONE or GATE…`, `/loop /flow-next`) exactly
as README has them; the `"autonomy": {…}` config example stays an inline span too. Nothing
path-shaped goes inside a fenced block (D-15).
`git add docs/autonomy.md` before running the gates.
  </action>
  <verify>
Run all four gate commands from MAPPING.md § Gate commands, verbatim:
G1 `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` — exit 0, last line `0 failures, N references checked` with N ≥ 140.
G2 `wc -l docs/*.md | awk '$2 != "total" && $1 > 250'` — prints nothing.
G3 the D-15 fence pipeline — prints nothing.
G4 `git diff --exit-code $(git merge-base main HEAD) -- NOTICE` — exit 0.
Then, for each of the six anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/autonomy.md`.
Then, one check per heading: `for h in '^## Autonomous operation' '^## Session hygiene'; do ! grep -q "$h" README.md || echo "STILL PRESENT: $h"; done` — prints nothing.
  </verify>
  <done>Both README sections live on `docs/autonomy.md` and neither remains in README; the page links `references/autonomy.md`; all four gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Move the acknowledgements verbatim and leave README a pointer</name>
  <files>docs/acknowledgements.md, README.md</files>
  <action>
Create `docs/acknowledgements.md` with an `# Acknowledgements` heading followed by all **8**
acknowledgement paragraphs from README's `## Acknowledgements` section — README lines 151–165 of the
phase-start blob, which is `git show e23403c:README.md`. They move **VERBATIM** (REQ-07): byte for
byte, same order, no rewording, no re-linking, no added sentences. The `# Acknowledgements` heading is
the only line you add. Every URL in them is external, so no link rewriting is needed or allowed.

In the same commit (D-14), remove those 8 paragraphs from README and replace them with **one**
pointer line under the retained `## Acknowledgements` heading, naming both destinations — the page
and `NOTICE` — for example: `Concept lineage and upstream credit: [docs/acknowledgements.md](docs/acknowledgements.md).
Required attributions are in `NOTICE`.` Keep README's final `MIT licensed — see LICENSE.` line.

**Do not touch `NOTICE`** — not a whitespace change, not a re-wrap. It is the legal artifact and REQ-07
requires it byte-identical; G4 proves it. Do not touch `LICENSE` either.
`git add docs/acknowledgements.md` before running the gates.
  </action>
  <verify>
The same four gate commands as Task 1 — they are the phase gate and run after every commit.
Then verbatim: `git show e23403c:README.md | sed -n '151,165p' | grep -v '^$' | while IFS= read -r l; do grep -qxF "$l" docs/acknowledgements.md || echo "NOT VERBATIM: $l"; done` — prints nothing.
Then, for each of the eight anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/acknowledgements.md`.
Then: `grep -A2 '^## Acknowledgements' README.md` shows a single pointer line naming `docs/acknowledgements.md` and `NOTICE`; `tail -1 README.md` is the MIT line.
Then: `ls docs/*.md | wc -l` → 11.
  </verify>
  <done>`docs/acknowledgements.md` carries all 8 paragraphs verbatim, README carries a one-line pointer plus the MIT line, `NOTICE` is untouched, and all four gates pass.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 02-05`. -->
