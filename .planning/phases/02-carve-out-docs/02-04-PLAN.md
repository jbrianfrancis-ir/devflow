---
phase: 02-carve-out-docs
plan: 04
wave: 4
depends_on: [02-03]
files_modified: [README.md, docs/review.md, docs/parallel-work.md]
autonomous: true
requirements: [REQ-05, REQ-12, REQ-12a, REQ-12b, REQ-12c, SC-03]
must_haves:
  truths:
    - "Each of these phrases has 0 hits in `README.md` and appears in exactly one file under `docs/`: `never force-pushes`, `same-model review reports identically`, `doesn't also get to rule`, `give the ledger its weight` (→ `docs/review.md`); `isn't decomposition`, `never screens`, `fighting over one checkout` (→ `docs/parallel-work.md`)."
    - "README contains no `## Many streams at once` section: `grep -cF 'Many streams at once' README.md` → 0, and every paragraph that stood under it now lives on exactly one of `docs/parallel-work.md`, `docs/review.md`, `docs/execution-model.md`."
    - "D-14 holds at **every** commit of this plan, not only at its end. Enumerate this plan's commits with `git log --format=%H --grep 'DevFlow-Plan: 02-04'` (the trailer this plan tags its commits with — see the commit note at the foot of this file), and for each sha and each anchor→page pair require the anchor to appear in exactly one of the two files — non-zero hits on one side, zero on the other. Hits on both means the prose sits in both files — the copy-now-delete-later window D-14 forbids; zero on both means it was dropped. Runnable, must print nothing: `shas=$(git log --format=%H --grep 'DevFlow-Plan: 02-04'); [ -n \"$shas\" ] || echo \"D-14 UNPROVEN: no commit carries DevFlow-Plan: 02-04\"; for sha in $shas; do for pair in 'never force-pushes|review' 'same-model review reports identically|review' \"doesn't also get to rule|review\" 'give the ledger its weight|review' \"isn't decomposition|parallel-work\" 'never screens|parallel-work' 'fighting over one checkout|parallel-work'; do a=${pair%|*}; p=${pair#*|}; r=$(git show $sha:README.md | grep -cF \"$a\" || true); d=$(git show $sha:docs/$p.md 2>/dev/null | grep -cF \"$a\" || true); { [ \"$r\" -eq 0 ] && [ \"$d\" -gt 0 ]; } || { [ \"$r\" -gt 0 ] && [ \"$d\" -eq 0 ]; } || echo \"D-14 VIOLATION $sha [$a] README=$r PAGE=$d\"; done; done`. The invariant is which side is non-zero rather than a sum: `grep -c` counts matching *lines*, so a correct page that carries an anchor on two lines — a sub-heading plus the prose, or one sentence per line — would fail a sum test. It is one-sided rather than a flat README=0 / page≥1 because each anchor moves in exactly one of this plan's commits; at the last sha every pair reads README=0, page≥1. The `shas` guard is not decoration: a zero-iteration loop prints nothing, which this truth would otherwise read as PASS, so a forgotten or mistyped trailer would silently prove nothing (fail-closed guards, conventions.md). `git show --stat` is not sufficient evidence here — it proves both files were touched, not that the README delta removed the prose the page gained."
    - "After every commit, `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` exits 0, and the checker's last line reports `0 failures` with at least 140 references checked."
    - "G3 from `.planning/phases/02-carve-out-docs/MAPPING.md` prints nothing: no path that resolves in this repo sits inside a fenced code block on `docs/review.md` or `docs/parallel-work.md` (D-15) — the fleet-board and ledger prose is the densest path-carrying material in the phase."
    - "`wc -l docs/*.md | awk '$2 != \"total\" && $1 > 250'` prints nothing (SC-03); `docs/review.md` now carries six README paragraphs and is the page at risk."
    - "`docs/review.md` links `../plugins/devflow/references/adjudication.md`; `docs/parallel-work.md` links `../plugins/devflow/references/conventions.md`. Neither restates the verdict/disposition vocabularies, the three ledger rules as normative text, or the workstream reconciliation table (REQ-12a)."
    - "`git ls-files docs/parallel-work.md` prints it."
    - "`git diff --exit-code $(git merge-base main HEAD) -- plugins/devflow/references/ NOTICE docs/blitzos.md docs/status-contract.md` exits 0."
  artifacts: [docs/parallel-work.md]
  key_links:
    - "`docs/review.md` gains its adversarial-review sections **after** the sections plan 02-03 wrote; the page keeps one `# Review` heading, not two."
    - "`docs/parallel-work.md` links `../plugins/devflow/references/conventions.md` → Parallel workstreams for the `.planning/` reconciliation rules rather than repeating the table."
---

<!-- Size note: this plan is ~10KB against plan-format's 4KB cap. Deliberate, on the same grounds
D-11 accepted for phase 01: the per-anchor move truths and the per-commit D-14 proof are what cost the
bytes, and they are what makes the phase verifiable by an executor with no memory of the planning
conversation. Do not shrink it by generalizing them. -->

<objective>
Finish docs/review.md with adversarial review, adjudication and PR-to-green, then carve the fleet
board and workstreams into docs/parallel-work.md — which empties and removes README's streams section.
</objective>

<context>
.planning/phases/02-carve-out-docs/MAPPING.md (the section→file mapping, gate commands G1–G4,
anchor phrases, and the measured link forms — read it first),
.planning/phases/02-carve-out-docs/CONTEXT.md, .planning/REQUIREMENTS.md, README.md,
docs/review.md (created by plan 02-03), plugins/devflow/references/adjudication.md,
plugins/devflow/references/conventions.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Finish docs/review.md — adversarial review and PR to green</name>
  <files>docs/review.md, README.md</files>
  <action>
Append to the existing `docs/review.md` (do not create a second `# Review` heading) four more README
paragraphs, located by anchor phrase, in this order:
1. Adversarial review — `same-model review reports identically` (peer-provider dispatch under an
   enforced sandbox; a missing or failing peer is `BLOCKED`, never a quiet fall back to native).
2. Adjudication in a third context — `doesn't also get to rule` (the session that received the
   findings is about to write the fixes; verdict and disposition are two axes, not one).
3. The three ledger rules — `give the ledger its weight` (disproving costs what proving cost; a
   deferral costs a backlog entry that already exists; a closed round is immutable, and shipping a
   `CONFIRMED` finding `ACCEPTED AS-IS` is a human gate).
4. Driving a PR to green — `never force-pushes` (`/flow-ci`: failure classification, bot-thread
   triage, and the four things it never does).
Remove all four from README **in this same commit** (D-14).

REQ-12: `adjudication.md` owns both vocabularies and the rules as normative text. Keep *why* the two
axes are separate and why each rule exists — that judgment is the reason the page is worth reading —
and link `../plugins/devflow/references/adjudication.md` for the enumerated values rather than
listing `CONFIRMED`/`REFUTED`/… and `FIX NOW`/`FIX LATER`/… in full. If the page nears 250 lines
(SC-03), compress under REQ-12a; never split it, since REQ-05 requires one home per topic.
  </action>
  <verify>
Run all four gate commands from MAPPING.md § Gate commands, verbatim:
G1 `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` — exit 0, last line `0 failures, N references checked` with N ≥ 140.
G2 `wc -l docs/*.md | awk '$2 != "total" && $1 > 250'` — prints nothing.
G3 the D-15 fence pipeline — prints nothing.
G4 `git diff --exit-code $(git merge-base main HEAD) -- NOTICE` — exit 0.
Then, for each of the four anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/review.md`.
Then: `grep -c '^# ' docs/review.md` → 1.
  </verify>
  <done>`docs/review.md` carries all six review surfaces under one `# Review` heading, README carries none of them, the page links `adjudication.md`, and all four gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Carve docs/parallel-work.md and retire README's streams section</name>
  <files>docs/parallel-work.md, README.md</files>
  <action>
Create `docs/parallel-work.md` with a `# Parallel work` heading from what is left of README's
`## Many streams at once` section:
1. The section's opening sentence — `isn't decomposition` — moves intact and leads the page.
2. Fleet board — `never screens` (`/flow-status --all` over `flow-fleet.py`: what a row shows,
   attention-first ordering, the needs-a-human footer, `--json`, exit status `1`, and the
   `~/.devflow/fleet.json` roots config).
3. Workstreams — `fighting over one checkout` (one git worktree per stream, refused streams that
   share a hidden edge, port offsets, the untracked files a new tree lacks).
Remove those three **and the `## Many streams at once` heading** from README in this same commit
(D-14). After this commit the section is gone entirely — its other paragraphs already left in plans
02-02 and 02-04 Task 1. If any remain, stop: an earlier task is incomplete.

**One deliberate deletion, and it must be recorded in the commit message** so phase 04's REQ-06 audit
finds the reason: the connector "Four pieces address that:" is dropped. It is already false in
today's README (seven bolded pieces follow it) and it is scaffolding for a section being dissolved
across three pages. No claim is lost — the sentence before it moves intact.

REQ-12: the fleet board has no reference counterpart and this page owns it outright (REQ-12b);
`docs/status-contract.md` is the existing external contract for `FLOW:` parsing and should be linked
as a sibling, `(status-contract.md)`, if referenced — `docs/status-contract.md` does not resolve from
inside `docs/`. Workstream reconciliation is owned by `conventions.md` → Parallel workstreams: link
`../plugins/devflow/references/conventions.md` and do not repeat the table.
Keep `~/.devflow/fleet.json` and its JSON example as inline code spans in prose, exactly as README
has them — not a fenced block (D-15).
`git add docs/parallel-work.md` before running the gates.
  </action>
  <verify>
The same four gate commands as Task 1 — they are the phase gate and run after every commit.
Then, for each of the three anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/parallel-work.md`.
Then: `grep -cF 'Many streams at once' README.md` → 0.
Then: `git log -1 --format=%B` contains the recorded reason for dropping "Four pieces address that:".
  </verify>
  <done>`docs/parallel-work.md` holds the intro sentence, the fleet board and workstreams; README's streams section no longer exists; the dropped connector is recorded in the commit message; all four gates pass.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 02-04`. -->
