---
phase: 02-carve-out-docs
plan: 02
wave: 2
depends_on: [02-01]
files_modified: [README.md, docs/execution-model.md, docs/provenance.md]
autonomous: true
requirements: [REQ-05, REQ-12, REQ-12a, REQ-12c, SC-03]
must_haves:
  truths:
    - "Each of these phrases has 0 hits in `README.md` and appears in exactly one file under `docs/`: `fresh-context executor per plan per wave`, `phase 5 silently breaks phase 2`, `hard size caps`, `Environment manifest`, `pins your exact stack`, `invented styles`, `mapped_sha` (→ `docs/execution-model.md`); `unreconstructable` (→ `docs/provenance.md`)."
    - "D-14 holds at **every** commit of this plan, not only at its end. Enumerate this plan's commits with `git log --format=%H --grep 'DevFlow-Plan: 02-02'` (the trailer this plan tags its commits with — see the commit note at the foot of this file), and for each sha and each anchor→page pair require the anchor to appear in exactly one of the two files — non-zero hits on one side, zero on the other. Hits on both means the prose sits in both files — the copy-now-delete-later window D-14 forbids; zero on both means it was dropped. Runnable, must print nothing: `shas=$(git log --format=%H --grep 'DevFlow-Plan: 02-02'); [ -n \"$shas\" ] || echo \"D-14 UNPROVEN: no commit carries DevFlow-Plan: 02-02\"; for sha in $shas; do for pair in 'fresh-context executor per plan per wave|execution-model' 'phase 5 silently breaks phase 2|execution-model' 'hard size caps|execution-model' 'Environment manifest|execution-model' 'pins your exact stack|execution-model' 'invented styles|execution-model' 'mapped_sha|execution-model' 'unreconstructable|provenance'; do a=${pair%|*}; p=${pair#*|}; r=$(git show $sha:README.md | grep -cF \"$a\" || true); d=$(git show $sha:docs/$p.md 2>/dev/null | grep -cF \"$a\" || true); { [ \"$r\" -eq 0 ] && [ \"$d\" -gt 0 ]; } || { [ \"$r\" -gt 0 ] && [ \"$d\" -eq 0 ]; } || echo \"D-14 VIOLATION $sha [$a] README=$r PAGE=$d\"; done; done`. The invariant is which side is non-zero rather than a sum: `grep -c` counts matching *lines*, so a correct page that carries an anchor on two lines — a sub-heading plus the prose, or one sentence per line — would fail a sum test. It is one-sided rather than a flat README=0 / page≥1 because each anchor moves in exactly one of this plan's commits; at the last sha every pair reads README=0, page≥1. The `shas` guard is not decoration: a zero-iteration loop prints nothing, which this truth would otherwise read as PASS, so a forgotten or mistyped trailer would silently prove nothing (fail-closed guards, conventions.md). `git show --stat` is not sufficient evidence here — it proves both files were touched, not that the README delta removed the prose the page gained."
    - "After every commit, `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` exits 0, and the checker's last line reports `0 failures` with at least 140 references checked."
    - "G3 from `.planning/phases/02-carve-out-docs/MAPPING.md` prints nothing: no path that resolves in this repo sits inside a fenced code block on either new page (D-15)."
    - "`wc -l docs/*.md | awk '$2 != \"total\" && $1 > 250'` prints nothing (SC-03) — `docs/execution-model.md` carries seven README paragraphs and is the page most likely to breach it."
    - "`git ls-files docs/execution-model.md docs/provenance.md` prints both."
    - "`docs/execution-model.md` links `../plugins/devflow/references/plan-format.md`, `../plugins/devflow/references/verification.md` and `../plugins/devflow/references/conventions.md`; `docs/provenance.md` links `../plugins/devflow/references/conventions.md`. No wave-arithmetic rule, must_haves field list, secret-scan pattern, or trailer field spec is restated on either page (REQ-12a)."
    - "`git diff --exit-code $(git merge-base main HEAD) -- plugins/devflow/references/` exits 0 — no reference file was edited (REQ-12c)."
  artifacts: [docs/execution-model.md, docs/provenance.md]
  key_links:
    - "Both pages resolve their reference links as `../plugins/devflow/references/<file>.md` — a `plugins/…` markdown link from inside `docs/` does not resolve."
    - "`docs/execution-model.md`'s smoke-gate section names `.planning/ARCHITECTURE.md` → `## Smoke` as the source of the command, and does not hardcode this repo's own smoke command."
---

<!-- Size note: this plan is ~10KB against plan-format's 4KB cap. Deliberate, on the same grounds
D-11 accepted for phase 01: the per-anchor move truths and the per-commit D-14 proof are what cost the
bytes, and they are what makes the phase verifiable by an executor with no memory of the planning
conversation. Do not shrink it by generalizing them. -->

<objective>
Carve the execution loop — graph execution, smoke gate, state, conventions, architecture and design
constraints, map staleness — and provenance out of README's Flow section into two docs/ pages.
</objective>

<context>
.planning/phases/02-carve-out-docs/MAPPING.md (the section→file mapping, gate commands G1–G4,
anchor phrases, and the measured link forms — read it first),
.planning/phases/02-carve-out-docs/CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ARCHITECTURE.md,
README.md, plugins/devflow/references/plan-format.md, plugins/devflow/references/verification.md,
plugins/devflow/references/conventions.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Carve docs/execution-model.md</name>
  <files>docs/execution-model.md, README.md</files>
  <action>
Create `docs/execution-model.md` with an `# Execution model` heading and move seven README paragraphs
into it, located by anchor phrase (never by line number — README shifts as this phase proceeds), in
this reading order:
1. Graph execution — `fresh-context executor per plan per wave`
2. Smoke gate — `phase 5 silently breaks phase 2`
3. State lives in `.planning/` — `hard size caps`
4. Docs that don't quietly rot (map staleness) — `mapped_sha`. It sits in README's "Many streams at
   once" section, but it is the phase-close step of the execute → verify loop; D-16 folds it here.
5. Conventions — `Environment manifest`
6. Architecture constraints — `pins your exact stack`
7. Design constraints — `invented styles`. D-16 sends it here: like architecture constraints it is a
   pin the planner and executor treat as law, and a violation is a verification gap.
Remove all seven from README **in this same commit** (D-14). Leave the `## Flow` heading, the ASCII
diagram, and the `/flow-oracle` paragraph (`high-stakes decision`) in place — the diagram stays
(REQUIREMENTS → Assumptions) and oracle belongs to plan 02-04.

REQ-12/D-10 governs paragraphs 1, 2, 3, 5 and 6: `plan-format.md` owns wave arithmetic, the fake-edge
test's exact statement, must_haves field lists and split signals; `verification.md` owns the
abstention and smoke-gate procedure; `conventions.md` owns layout, git workflow, the secret-scan
pattern and the Environment manifest rules. Keep what each thing *is* and *why it exists* — the
narrative sentences move intact — and replace enumerated rules with a link. Paragraphs 4 and 7 have
no reference counterpart and are owned outright by this page.

If the page approaches 250 lines (SC-03), compress further under REQ-12a. Never split it: REQ-05
requires one home per topic.

All path references stay in prose; nothing path-shaped goes inside a fenced block (D-15).
`git add docs/execution-model.md` before running the gates.
  </action>
  <verify>
Run all four gate commands from MAPPING.md § Gate commands, verbatim:
G1 `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` — exit 0, last line `0 failures, N references checked` with N ≥ 140.
G2 `wc -l docs/*.md | awk '$2 != "total" && $1 > 250'` — prints nothing.
G3 the D-15 fence pipeline — prints nothing.
G4 `git diff --exit-code $(git merge-base main HEAD) -- NOTICE` — exit 0.
Then, for each of the seven anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/execution-model.md`.
Then, one check per reference (`grep -c` with an alternation counts matching *lines*, not distinct references): `for r in plan-format verification conventions; do grep -q "references/$r.md" docs/execution-model.md || echo "MISSING $r"; done` — prints nothing.
  </verify>
  <done>All seven paragraphs live on `docs/execution-model.md` and none remain in README; the page links its three references and is under 250 lines; all four gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Carve docs/provenance.md</name>
  <files>docs/provenance.md, README.md</files>
  <action>
Create `docs/provenance.md` with a `# Provenance` heading from README's provenance paragraph (anchor
`unreconstructable`) — "who made which change, and who approved it": commit trailers,
`.planning/DECISIONS.md` as the append-only gate record, `JOURNAL.md` overflow rolling into
`.planning/history/`, and `/flow-audit --export`. Remove the paragraph from README in this same
commit (D-14).

This is one long README paragraph and the page should break it into sections — attribution, the
decision log, the export — but the sentences themselves move intact. Two claims are load-bearing and
must survive verbatim in substance: the committer stays the human whose credentials made the commit
(the trailer records what assisted, not who is accountable), and the export leads with what it
*cannot* cover, because a pack that overstates itself is worth less than none.

REQ-12: `conventions.md` owns `## Commit attribution` (exact trailer syntax and field rules) and
`## Decision log`; `autonomy.md` owns the authoritative human-gate list. Link both as
`../plugins/devflow/references/conventions.md` and `../plugins/devflow/references/autonomy.md`. One
illustrative trailer pair may appear inline as code spans, as README has it — do not promote it to a
fenced block, and do not reproduce the trailer field table.

`git add docs/provenance.md` before running the gates.
  </action>
  <verify>
The same four gate commands as Task 1 — they are the phase gate and run after every commit.
Then: `grep -cF "unreconstructable" README.md` → 0 and `grep -rlF "unreconstructable" docs/` → `docs/provenance.md`.
Then: `grep -n 'references/conventions.md' docs/provenance.md` prints a line, and G1 proves it resolves.
Then, at this commit: `git show HEAD:README.md | grep -cF unreconstructable` → 0 and `git show HEAD:docs/provenance.md | grep -cF unreconstructable` → 1 (the removal and the addition are the same commit — `--stat` would only show both files were touched).
  </verify>
  <done>`docs/provenance.md` exists, links `conventions.md` and `autonomy.md`, README's Flow section is down to the heading, the diagram and the oracle paragraph, and all four gates pass.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 02-02`. -->
