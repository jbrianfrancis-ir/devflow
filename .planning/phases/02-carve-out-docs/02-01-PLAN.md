---
phase: 02-carve-out-docs
plan: 01
wave: 1
depends_on: []
files_modified: [README.md, docs/installation.md, docs/providers.md]
autonomous: true
requirements: [REQ-05, REQ-12, REQ-12a, REQ-12b, REQ-12c, SC-03]
must_haves:
  truths:
    - "Each of these phrases has 0 hits in `README.md` and appears in exactly one file under `docs/`: `Codex cloud is not`, `self-bootstrapping`, `company-brain rendering` (→ `docs/installation.md`); `bounded repository context`, `cost is a property of the plugin` (→ `docs/providers.md`). Check: `grep -cF "<phrase>" README.md` → 0, `grep -rlF "<phrase>" docs/` → one path."
    - "D-14 holds at **every** commit of this plan, not only at its end. Enumerate this plan's commits with `git log --format=%H --grep 'DevFlow-Plan: 02-01'` (the trailer this plan tags its commits with — see the commit note at the foot of this file), and for each sha and each anchor→page pair require the anchor to appear in exactly one of the two files — non-zero hits on one side, zero on the other. Hits on both means the prose sits in both files — the copy-now-delete-later window D-14 forbids; zero on both means it was dropped. Runnable, must print nothing: `shas=$(git log --format=%H --grep 'DevFlow-Plan: 02-01'); [ -n \"$shas\" ] || echo \"D-14 UNPROVEN: no commit carries DevFlow-Plan: 02-01\"; for sha in $shas; do for pair in 'Codex cloud is not|installation' 'self-bootstrapping|installation' 'company-brain rendering|installation' 'bounded repository context|providers' 'cost is a property of the plugin|providers'; do a=${pair%|*}; p=${pair#*|}; r=$(git show $sha:README.md | grep -cF \"$a\" || true); d=$(git show $sha:docs/$p.md 2>/dev/null | grep -cF \"$a\" || true); { [ \"$r\" -eq 0 ] && [ \"$d\" -gt 0 ]; } || { [ \"$r\" -gt 0 ] && [ \"$d\" -eq 0 ]; } || echo \"D-14 VIOLATION $sha [$a] README=$r PAGE=$d\"; done; done`. The invariant is which side is non-zero rather than a sum: `grep -c` counts matching *lines*, so a correct page that carries an anchor on two lines — a sub-heading plus the prose, or one sentence per line — would fail a sum test. It is one-sided rather than a flat README=0 / page≥1 because each anchor moves in exactly one of this plan's commits; at the last sha every pair reads README=0, page≥1. The `shas` guard is not decoration: a zero-iteration loop prints nothing, which this truth would otherwise read as PASS, so a forgotten or mistyped trailer would silently prove nothing (fail-closed guards, conventions.md). `git show --stat` is not sufficient evidence here — it proves both files were touched, not that the README delta removed the prose the page gained."
    - "After every commit, `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` exits 0, and the checker's last line reports `0 failures` with at least 140 references checked."
    - "G3 from `.planning/phases/02-carve-out-docs/MAPPING.md` prints nothing: no path that resolves in this repo sits inside a fenced code block on either new page (D-15)."
    - "`wc -l docs/*.md | awk '$2 != \"total\" && $1 > 250'` prints nothing (SC-03)."
    - "`git ls-files docs/installation.md docs/providers.md` prints both — an untracked page is invisible to check-links.py, so a green smoke over one proves nothing."
    - "`docs/providers.md` links `../plugins/devflow/references/hosts.md` and `docs/installation.md` links `../plugins/devflow/references/conventions.md`; neither restates a rule those references define (REQ-12a). Settled, not conditional: D-17 corrected REQ-12b's enumeration — `hosts.md` is the reference counterpart for providers and model tiers — so REQ-12's summarize-and-link condition governs, and `docs/providers.md` does not restate the provider-precedence rules or the model-tier assignments."
    - "`docs/blitzos.md`, `docs/status-contract.md` and every `references/*.md` are unchanged: `git diff --exit-code $(git merge-base main HEAD) -- docs/blitzos.md docs/status-contract.md plugins/devflow/references/` exits 0 (REQ-12c)."
  artifacts: [docs/installation.md, docs/providers.md]
  key_links:
    - "`docs/installation.md`'s context-repo paragraph links `blitzos.md` as a sibling — NOT `docs/blitzos.md`, which does not resolve from inside `docs/`."
    - "Both pages resolve their reference links as `../plugins/devflow/references/<file>.md`."
---

<!-- Size note: this plan is ~10KB against plan-format's 4KB cap. Deliberate, on the same grounds
D-11 accepted for phase 01: the per-anchor move truths and the per-commit D-14 proof are what cost the
bytes, and they are what makes the phase verifiable by an executor with no memory of the planning
conversation. Do not shrink it by generalizing them. -->

<objective>
Carve installation depth and provider/model-tier depth out of README's Install section into two
docs/ pages. README keeps both install blocks and their headings; phase 03 owns its final shape.
</objective>

<context>
.planning/phases/02-carve-out-docs/MAPPING.md (the section→file mapping, gate commands G1–G4,
anchor phrases, and the measured link forms — read it first),
.planning/phases/02-carve-out-docs/CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ARCHITECTURE.md,
README.md, plugins/devflow/references/hosts.md, plugins/devflow/references/conventions.md,
docs/blitzos.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Carve docs/installation.md</name>
  <files>docs/installation.md, README.md</files>
  <action>
Create `docs/installation.md` with an `# Installation` heading and move three README paragraphs into
it, in this order (locate them by their anchor phrase, never by line number — README line numbers
shift as this phase proceeds):
1. The Codex paragraph "Start a new Codex thread after installation…" (anchor `Codex cloud is not`).
2. The self-bootstrap paragraph "Claude projects remain **self-bootstrapping**…" (anchor
   `self-bootstrapping`) — it sits under README's `### Model tiers` heading but is bootstrap, not
   tiers, and belongs here.
3. The context-repo paragraph "**Context repos (BlitzOS-style)**…" (anchor `company-brain rendering`).
Remove all three from README **in this same commit** (D-14). Do not touch README's `## Install`
heading, the `### Claude Code` / `### Codex CLI, app, or IDE` headings, or either fenced install
block — REQ-03 keeps those, and phase 03 owns the final Install section.

Register is preserved: these sentences move intact (REQUIREMENTS.md → Assumptions). Add only section
headings and the link rewrites below.

Link rewrites, required — verified against this checker:
- The BlitzOS contract link's target changes from docs/blitzos.md to the sibling form blitzos.md
  (link text may stay as it is). A markdown link resolves against the referring file's own directory
  only, so a docs/blitzos.md target 404s from inside docs/ — measured, see MAPPING.md § Link forms.
- Under REQ-12, add a one-line pointer to `../plugins/devflow/references/conventions.md` for the
  self-bootstrap `.claude/settings.json` block and the `CLAUDE.md`/`AGENTS.md` merge semantics, and
  do not restate either — summarize what it is and why it exists, then link.
- Keep every path reference in prose. Nothing path-shaped goes inside a fenced block (D-15).
`git add docs/installation.md` before running the gates, or the checker cannot see it.
  </action>
  <verify>
Run all four gate commands from MAPPING.md § Gate commands, verbatim:
G1 `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` — exit 0, last line `0 failures, N references checked` with N ≥ 140.
G2 `wc -l docs/*.md | awk '$2 != "total" && $1 > 250'` — prints nothing.
G3 the D-15 fence pipeline — prints nothing.
G4 `git diff --exit-code $(git merge-base main HEAD) -- NOTICE` — exit 0.
Then, per anchor: `grep -cF "Codex cloud is not" README.md` → 0 and `grep -rlF "Codex cloud is not" docs/` → `docs/installation.md`; same for `self-bootstrapping` and `company-brain rendering`.
  </verify>
  <done>`docs/installation.md` holds the three paragraphs, README holds none of them, all four gates pass, and at this commit each of the three anchors reads 0 in `README.md` and 1 in `docs/installation.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Carve docs/providers.md</name>
  <files>docs/providers.md, README.md</files>
  <action>
Create `docs/providers.md` with a `# Providers and model tiers` heading, carrying two sections built
from README's `### Provider selection` body (anchor `bounded repository context`) and `### Model
tiers` body (anchor `cost is a property of the plugin`). Remove both headings and both bodies from
README in this same commit (D-14). The self-bootstrap and context-repo paragraphs that sit under
`### Model tiers` are already gone in Task 1 — if either is still present, stop: Task 1 is incomplete.

REQ-12 applies here — settled by D-17, which corrected REQ-12b's enumeration (see MAPPING.md → REQ-12b).
`hosts.md` owns
`## Provider selection and dispatch`, `## Model tiers`, and `## Cross-provider safety`. So this page
states what provider selection is, why it exists, and enough shape to decide whether to read further
— the flag/config precedence in one line, which role tiers are top vs a tier down and why the
executor is deliberately cheap — and links `../plugins/devflow/references/hosts.md` as source of
truth. Do not restate the per-role model table, the dispatch mechanics, or the sandbox rules; a page
that contradicts its reference is a defect, not staleness.

Config snippets (`"agents": {"provider": …}`, `"agents": {"models": {"executor": "opus"}}`) stay
inline code spans in prose exactly as README has them — do not promote them to fenced blocks (D-15).
`git add docs/providers.md` before running the gates.
  </action>
  <verify>
The same four gate commands as Task 1 — they are the phase gate and run after every commit.
Then: `grep -cF "bounded repository context" README.md` → 0 and `grep -rlF … docs/` → `docs/providers.md`; same for `cost is a property of the plugin`.
Then: `grep -n 'references/hosts.md' docs/providers.md` prints a line, and G1 proves it resolves.
Then: `grep -n '^### Provider selection\|^### Model tiers' README.md` prints nothing.
  </verify>
  <done>`docs/providers.md` exists and links `hosts.md`, README's Install section is down to its heading, two sub-headings and two install blocks, and all four gates pass.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 02-01`. -->
