---
phase: 02-carve-out-docs
plan: 03
wave: 3
depends_on: [02-02]
files_modified: [README.md, docs/requirements-clarity.md, docs/review.md]
autonomous: true
requirements: [REQ-05, REQ-12, REQ-12a, REQ-12c, SC-03]
must_haves:
  truths:
    - "Each of these phrases has 0 hits in `README.md` and appears in exactly one file under `docs/`: `confident sentences`, `asked at each cheap moment`, `too load-bearing to be wrong`, `work nobody asked for` (→ `docs/requirements-clarity.md`); `high-stakes decision`, `cheapest moment in the whole workflow`, `grades its own homework` (→ `docs/review.md`)."
    - "D-14 holds at **every** commit of this plan, not only at its end. Enumerate this plan's commits with `git log --format=%H --grep 'DevFlow-Plan: 02-03'` (the trailer this plan tags its commits with — see the commit note at the foot of this file), and for each sha and each anchor→page pair require the anchor to appear in exactly one of the two files — non-zero hits on one side, zero on the other. Hits on both means the prose sits in both files — the copy-now-delete-later window D-14 forbids; zero on both means it was dropped. Runnable, must print nothing: `shas=$(git log --format=%H --grep 'DevFlow-Plan: 02-03'); [ -n \"$shas\" ] || echo \"D-14 UNPROVEN: no commit carries DevFlow-Plan: 02-03\"; for sha in $shas; do for pair in 'confident sentences|requirements-clarity' 'asked at each cheap moment|requirements-clarity' 'too load-bearing to be wrong|requirements-clarity' 'work nobody asked for|requirements-clarity' 'high-stakes decision|review' 'cheapest moment in the whole workflow|review' 'grades its own homework|review'; do a=${pair%|*}; p=${pair#*|}; r=$(git show $sha:README.md | grep -cF \"$a\" || true); d=$(git show $sha:docs/$p.md 2>/dev/null | grep -cF \"$a\" || true); { [ \"$r\" -eq 0 ] && [ \"$d\" -gt 0 ]; } || { [ \"$r\" -gt 0 ] && [ \"$d\" -eq 0 ]; } || echo \"D-14 VIOLATION $sha [$a] README=$r PAGE=$d\"; done; done`. The invariant is which side is non-zero rather than a sum: `grep -c` counts matching *lines*, so a correct page that carries an anchor on two lines — a sub-heading plus the prose, or one sentence per line — would fail a sum test. It is one-sided rather than a flat README=0 / page≥1 because each anchor moves in exactly one of this plan's commits; at the last sha every pair reads README=0, page≥1. The `shas` guard is not decoration: a zero-iteration loop prints nothing, which this truth would otherwise read as PASS, so a forgotten or mistyped trailer would silently prove nothing (fail-closed guards, conventions.md). `git show --stat` is not sufficient evidence here — it proves both files were touched, not that the README delta removed the prose the page gained."
    - "After every commit, `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` exits 0, and the checker's last line reports `0 failures` with at least 140 references checked."
    - "G3 from `.planning/phases/02-carve-out-docs/MAPPING.md` prints nothing: no path that resolves in this repo sits inside a fenced code block on either new page (D-15)."
    - "`wc -l docs/*.md | awk '$2 != \"total\" && $1 > 250'` prints nothing (SC-03)."
    - "`git ls-files docs/requirements-clarity.md docs/review.md` prints both."
    - "`docs/requirements-clarity.md` links `../plugins/devflow/references/plan-format.md` and `../plugins/devflow/references/verification.md`; `docs/review.md` links `../plugins/devflow/references/oracle.md` and `../plugins/devflow/references/plan-format.md`. Neither restates the `backstop_truths` field rules, the abstention procedure, or the consult-bundle mechanics those references define (REQ-12a)."
    - "README no longer contains a `## Saying \"unknown\" out loud` heading: `grep -cF 'Saying \"unknown\" out loud' README.md` → 0."
    - "`git diff --exit-code $(git merge-base main HEAD) -- plugins/devflow/references/ NOTICE` exits 0 (REQ-12c, REQ-07)."
  artifacts: [docs/requirements-clarity.md, docs/review.md]
  key_links:
    - "Both pages resolve their reference links as `../plugins/devflow/references/<file>.md` — a `plugins/…` markdown link from inside `docs/` does not resolve."
    - "`docs/review.md` is created here and **appended to** by plan 02-04; its section order must leave adversarial review and PR-to-green to be added after the sections written here."
---

<!-- Size note: this plan is ~10KB against plan-format's 4KB cap. Deliberate, on the same grounds
D-11 accepted for phase 01: the per-anchor move truths and the per-commit D-14 proof are what cost the
bytes, and they are what makes the phase verifiable by an executor with no memory of the planning
conversation. Do not shrink it by generalizing them. -->

<objective>
Carve the requirements-clarity chain out of README, and open docs/review.md with the three review
surfaces that are not adversarial review: the plan panel, the diff-review crew, and second opinions.
</objective>

<context>
.planning/phases/02-carve-out-docs/MAPPING.md (the section→file mapping, gate commands G1–G4,
anchor phrases, and the measured link forms — read it first),
.planning/phases/02-carve-out-docs/CONTEXT.md, .planning/REQUIREMENTS.md, README.md,
plugins/devflow/references/plan-format.md, plugins/devflow/references/verification.md,
plugins/devflow/references/questioning.md, plugins/devflow/references/oracle.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Carve docs/requirements-clarity.md</name>
  <files>docs/requirements-clarity.md, README.md</files>
  <action>
Create `docs/requirements-clarity.md` with a `# Requirements clarity` heading from README's whole
`## Saying "unknown" out loud` section — its heading and all four paragraphs, located by anchor
phrase: `confident sentences` (why agents assert), `asked at each cheap moment`
(`[NEEDS CLARIFICATION]` markers → `backstop_truths` → verifier abstention),
`too load-bearing to be wrong` (`## Assumptions` and `SC-NN` success criteria), `work nobody asked
for` (`/flow-audit`). Remove the heading and all four from README **in this same commit** (D-14).

The section's argument is the page's argument and its order is already right — move the sentences
intact, add sub-headings, change nothing else. README's own section title is a good page subtitle but
the `# ` heading should name the topic REQ-05 names.

REQ-12: `plan-format.md` owns `### backstop_truths` (what makes a truth non-inferable, the structured
tag rule, why the list stays small) and `verification.md` owns `## backstop truths — abstain, don't
certify`; `questioning.md` owns the intake questions. Keep the chain — spec didn't say → marker →
backstop truth → abstention — and link rather than restate the field rules. The example marker
(`- REQ-03: authenticates users via [NEEDS CLARIFICATION: …]`) stays an inline code span in prose as
README has it; do not promote it to a fenced block (D-15).
`git add docs/requirements-clarity.md` before running the gates.
  </action>
  <verify>
Run all four gate commands from MAPPING.md § Gate commands, verbatim:
G1 `python3 scripts/validate-plugin.py && python3 -m unittest discover -s tests -v && python3 scripts/check-links.py` — exit 0, last line `0 failures, N references checked` with N ≥ 140.
G2 `wc -l docs/*.md | awk '$2 != "total" && $1 > 250'` — prints nothing.
G3 the D-15 fence pipeline — prints nothing.
G4 `git diff --exit-code $(git merge-base main HEAD) -- NOTICE` — exit 0.
Then, for each of the four anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/requirements-clarity.md`.
Then: `grep -cF 'Saying "unknown" out loud' README.md` → 0.
  </verify>
  <done>The whole section lives on `docs/requirements-clarity.md`, README has no trace of it, the page links `plan-format.md` and `verification.md`, and all four gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Open docs/review.md — plan panel, review crew, second opinions</name>
  <files>docs/review.md, README.md</files>
  <action>
Create `docs/review.md` with a `# Review` heading and move three README paragraphs into it, located
by anchor phrase, in this reading order (earliest moment first):
1. Review before the code exists — `cheapest moment in the whole workflow` (`/flow-plan N --panel`,
   the three lenses beside `flow-plan-checker`).
2. Review that isn't self-review — `grades its own homework` (`/flow-pr`'s parallel `flow-reviewer`
   lenses, dedupe and `blocking`/`should-fix`/`nit` classification, refutation as a human gate, the
   PR body's Review guide).
3. Second opinions — `high-stakes decision` (`/flow-oracle`). D-16 folds it here: it is external
   judgment on work in progress, and it is the escalation the other two reach for.
Remove all three from README **in this same commit** (D-14). Paragraph 3 sits in README's `## Flow`
section, the other two in `## Many streams at once`; leave both headings alone — plan 02-04 removes
the streams heading with its last remaining content.

Leave room after these sections: plan 02-04 appends adversarial review, adjudication, and PR-to-green
to this page.

REQ-12: `oracle.md` owns the bundle, engine, panel and consult mechanics — state what a second
opinion is, why the secret scan and human gate apply, and link it. `plan-format.md` owns the revision
gate's round budget. Keep the judgment that makes each section worth reading — the panel answers *is
this the right plan* where the checker answers *is this plan executable*; a blocking finding demands
a concrete failure scenario; a refuted blocking finding is a human gate — and link the rules.
`git add docs/review.md` before running the gates.
  </action>
  <verify>
The same four gate commands as Task 1 — they are the phase gate and run after every commit.
Then, for each of the three anchors: `grep -cF "<anchor>" README.md` → 0 and `grep -rlF "<anchor>" docs/` → `docs/review.md`.
Then: `grep -n 'references/oracle.md' docs/review.md` prints a line, and G1 proves it resolves.
Then, one check per phrase: `for a in 'same-model review reports identically' 'never force-pushes'; do grep -qF "$a" README.md || echo "MOVED EARLY: $a"; done` — prints nothing; plan 02-04's paragraphs are still in README.
  </verify>
  <done>`docs/review.md` exists with the three sections, README has none of them, the page links `oracle.md`, and all four gates pass.</done>
</task>
</tasks>

<!-- Commits: one per task; trailers `DevFlow-Agent: executor/claude/opus`, `DevFlow-Plan: 02-03`. -->
