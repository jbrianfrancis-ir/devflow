<!-- .planning/TODOS.md — captured ideas and small tasks, not roadmap work. Newest first.
Format: `- [ ] YYYY-MM-DD | one line | why it matters`. Promote to a phase or /flow-quick when acted on. -->
# Todos

- [x] 2026-09-09 | False Green R4-R9 — the six requirements quick 014 did not take (DONE: quick 015) | Quick 014 covers only the P0 set (R1 falsify clause, R2 execution-only assertion, R3 revision deletion diff). Remaining, in the spec's own order: **R4** a read-only `flow-prober` role that builds a throwaway project to test one toolchain assumption before it becomes an ARCHITECTURE pin — change one variable per probe, since the first probe in the source phase moved three and credited the wrong one; **R5** resolving a `backstop_truth` must land as a `must_haves.truths` entry and the checker re-scans every plan in the phase for contradicting sites, reporting all of them; **R6** the checker resolves every `NN-MM` mention in plan prose — a reference to a nonexistent plan, or one asserting another plan does X where X is in neither its `files_modified` nor its truths, is blocking; **R7** write roles emit to `.tmp` and atomically rename, and `hosts.md` states that a phase dir is never diffed while its write role runs (a quiet directory is not a finished one); **R8** an optional regex on an `ARCHITECTURE.md` Forbidden entry, applied by the existing pre-commit/pre-push scans across tracked files including `.planning/`, reporting pattern class never the matched value; **R9** a supported split-and-renumber operation for over-cap plans, plus re-running the size check after any hand edit. R4 needs a new agent contract plus both manifests; R8 touches `flow-hooks` and its tests; R9 likely wants a script. Roadmap-sized together — a phase 05, not a quick.

- [x] 2026-09-08 | `.planning/ARCHITECTURE.md:31-32` calls `check-links.py` "the standing CI gate" — PR #36 added a second one, so the claim is now false | `lint.yml` runs four steps in the `validate` job; the fourth, "Check version bump on shipped content", runs `scripts/check-version-bump.py "origin/$BASE_REF"` with `BASE_REF: ${{ github.base_ref }}` — PR-only, since `github.base_ref` is empty on push. ARCHITECTURE.md is law and names neither the gate nor that PR-only scope, so nothing binds the behaviour or warns that a direct push to main bypasses it entirely. This is the second of the two human items from #36's body; the first is the branch-protection gate now in STATE.
- [x] 2026-09-08 | Version examples in `plugins/devflow/templates/architecture.md:9-11` are stale — `.NET 9.0` / `C# 13` should be `.NET 10` / `C# 14`, and the Aspire example wants a feed check | This template seeds every new project's `ARCHITECTURE.md`, which CLAUDE.md calls "law, not advice" — a stale `{e.g. …}` gets copied into a real pin and then binds executors to a superseded runtime. Only the Aspire row carries a "pin what the feed actually has" note (`:12-13`); Runtime and Language have no equivalent, so nothing tells the author to check. Fix the numbers, and consider extending the resolve-from-the-feed instruction to the whole table so it stops going stale between DevFlow releases. Same staleness class as `references/aspire.md`, which already handles it explicitly.
- [ ] 2026-09-01 | `flow-ci/SKILL.md:18` calls `gh pr checks <n> --json name,state,bucket,link`, but `--json` is not a valid flag on `gh pr checks` in `gh` 2.46.0 (confirmed live: `unknown flag: --json`) | Found while adding a live-CI-check read to `flow-triager.md` for quick 012 and hitting the same failure — used `gh pr view <n> --json statusCheckRollup` there instead, which does work on this version. `flow-ci` predates this PR and is out of scope for it, but every invocation of that line fails the same way; swap it to the working query.
- [ ] 2026-08-27 | Nothing writes `config.json` → `git.branch` when a skill creates a `flow/<slug>` branch | `/flow-pr`'s pre-flight checks "on the feature branch (`git.branch`)" against that field. In this repo it had been stale since `flow/deploy-na-routing` — eight branches back — so the check was comparing against a branch that no longer mattered and would either pass vacuously or fail on correct work. Same cache-vs-fact shape as quick 011, but the stale value is DevFlow's own, which makes it a maintenance gap rather than an external-state one: either write the field when a branch is created, or drop it and read `git branch --show-current`.
